// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * App is the root service exposed to the frontend.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as backup$0 from "./internal/backup/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as creds$0 from "./internal/creds/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as exporter$0 from "./internal/exporter/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as httpc$0 from "./internal/httpc/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as mobaxterm$0 from "./internal/importer/mobaxterm/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as puttyreg$0 from "./internal/importer/puttyreg/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as rdm$0 from "./internal/importer/rdm/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as sshconfig$0 from "./internal/importer/sshconfig/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as recorder$0 from "./internal/recorder/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as ssh$0 from "./internal/ssh/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as store$0 from "./internal/store/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as syncer$0 from "./internal/syncer/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as updater$0 from "./internal/updater/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as wg$0 from "./internal/wg/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * AppClearLogs empties the ring. Live emit stops mid-stream so the
 * frontend just calls AppGetLogs() to confirm the buffer is empty.
 * @returns {$CancellablePromise<void>}
 */
export function AppClearLogs() {
    return $Call.ByID(4018418018);
}

/**
 * AppGetLogTailEnabled / AppSetLogTailEnabled control whether the log
 * buffer collects lines + emits live events. When off, log.Printf still
 * writes to stdout but the ring stays frozen and the UI gets no
 * notifications.
 * @returns {$CancellablePromise<boolean>}
 */
export function AppGetLogTailEnabled() {
    return $Call.ByID(986233813);
}

/**
 * AppGetLogs returns a snapshot of the in-app log ring (oldest first).
 * Frontend mounts it once and then appends live entries via the
 * "app_log" Wails event.
 * @returns {$CancellablePromise<string[]>}
 */
export function AppGetLogs() {
    return $Call.ByID(1266875363).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * AppRelaunch spawns a fresh instance of this binary and quits the
 * current one - the "restart to apply" step after a sync pull or
 * backup restore, without making the user find the icon again. The
 * child gets SSH_TOOL_WAIT_PID so its startup waits for this process
 * to release store.db (and so it doesn't hand itself off to us via
 * the single-instance socket and exit).
 * @returns {$CancellablePromise<void>}
 */
export function AppRelaunch() {
    return $Call.ByID(3000504262);
}

/**
 * @param {boolean} on
 * @returns {$CancellablePromise<void>}
 */
export function AppSetLogTailEnabled(on) {
    return $Call.ByID(947678673, on);
}

/**
 * AppVersion returns the build-time injected name / version / commit
 * plus the runtime-resolved schema version (from store/schema_meta).
 * @returns {$CancellablePromise<$models.AppVersionInfo>}
 */
export function AppVersion() {
    return $Call.ByID(1970741496).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType1($result);
    }));
}

/**
 * ApplyUpdate triggers the swap-and-restart helper (Windows only) and
 * quits the app. On Unix the rename already happened during Download
 * and we just exit so the user's next launch picks up the new binary.
 * Like DownloadUpdate it takes no parameters - the script path comes
 * from the backend's own Download, never from the webview.
 * 
 * Caller (frontend) is expected to have already closed user-visible
 * state; we still allow ~250 ms for in-flight IPC to flush before
 * calling os.Exit.
 * @returns {$CancellablePromise<void>}
 */
export function ApplyUpdate() {
    return $Call.ByID(4175318290);
}

/**
 * @param {$models.AuditListInput} $in
 * @returns {$CancellablePromise<store$0.AuditEvent[]>}
 */
export function AuditList($in) {
    return $Call.ByID(1521672216, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType3($result);
    }));
}

/**
 * AuditPurge drops all rows older than `olderThanDays`. Returns the
 * number of rows removed.
 * @param {number} olderThanDays
 * @returns {$CancellablePromise<number>}
 */
export function AuditPurge(olderThanDays) {
    return $Call.ByID(1961731999, olderThanDays);
}

/**
 * @returns {$CancellablePromise<$models.AutoBackupPrefs>}
 */
export function AutoBackupPrefsGet() {
    return $Call.ByID(1148048766).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType4($result);
    }));
}

/**
 * @param {$models.AutoBackupPrefs} p
 * @returns {$CancellablePromise<void>}
 */
export function AutoBackupPrefsSet(p) {
    return $Call.ByID(2994464226, p);
}

/**
 * BackupsCreate writes an encrypted snapshot of store.db + vault.enc.
 * The passphrase must match the live vault passphrase; we verify by
 * re-unlocking the vault file (cheap because it does not touch process
 * state).
 * @param {$models.BackupCreateInput} $in
 * @returns {$CancellablePromise<$models.BackupCreateResult | null>}
 */
export function BackupsCreate($in) {
    return $Call.ByID(3442444408, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType6($result);
    }));
}

/**
 * @param {string} path
 * @returns {$CancellablePromise<void>}
 */
export function BackupsDelete(path) {
    return $Call.ByID(169492519, path);
}

/**
 * @returns {$CancellablePromise<backup$0.Info[]>}
 */
export function BackupsList() {
    return $Call.ByID(1170052212).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType8($result);
    }));
}

/**
 * BackupsRestore overwrites the live store.db + vault.enc with the
 * contents of the backup at SrcPath. The caller must restart the app
 * for the new files to take effect (in-process SQLite handle keeps
 * pointing at the old inode otherwise).
 * @param {$models.BackupRestoreInput} $in
 * @returns {$CancellablePromise<void>}
 */
export function BackupsRestore($in) {
    return $Call.ByID(4165637462, $in);
}

/**
 * BatchExec runs one command across multiple connections in parallel.
 * Each host opens its own quiet SSH chain (no PTY, no scrollback) and
 * captures stdout/stderr/exit. Concurrency capped at 8. Returns a
 * per-host result slice in the same order as the input ids.
 * @param {$models.BatchExecInput} $in
 * @returns {$CancellablePromise<ssh$0.BatchHostResult[]>}
 */
export function BatchExec($in) {
    return $Call.ByID(1579859046, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType10($result);
    }));
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastAdd(sessionID) {
    return $Call.ByID(459624039, sessionID);
}

/**
 * BroadcastAddTo adds a session to the named group. Empty groupID
 * is the legacy default group.
 * @param {string} groupID
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastAddTo(groupID, sessionID) {
    return $Call.ByID(2402757074, groupID, sessionID);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastClear() {
    return $Call.ByID(1342316049);
}

/**
 * BroadcastClearGroup empties one group but keeps it defined.
 * @param {string} groupID
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastClearGroup(groupID) {
    return $Call.ByID(1196972232, groupID);
}

/**
 * BroadcastFanOut writes dataB64 to every member except originID. Server-
 * side fan-out means a keystroke from any window reaches every other
 * session even when the originating window is detached and the target
 * session lives in a different window's pane tree. Returns an error
 * summary string per failing member (empty on full success).
 * @param {string} originID
 * @param {string} dataB64
 * @returns {$CancellablePromise<string>}
 */
export function BroadcastFanOut(originID, dataB64) {
    return $Call.ByID(2752231907, originID, dataB64);
}

/**
 * BroadcastGroupDelete removes the named group entirely. No-op for
 * the default group ("") - clear it instead.
 * @param {string} groupID
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastGroupDelete(groupID) {
    return $Call.ByID(4167565164, groupID);
}

/**
 * BroadcastList returns the legacy default-group member ids
 * (unordered). Frontend code that hasn't migrated to the
 * multi-group API keeps working.
 * @returns {$CancellablePromise<string[]>}
 */
export function BroadcastList() {
    return $Call.ByID(1343885156).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * BroadcastListGroup returns the members of one named group.
 * @param {string} groupID
 * @returns {$CancellablePromise<string[]>}
 */
export function BroadcastListGroup(groupID) {
    return $Call.ByID(3883208151, groupID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * BroadcastListGroups returns the full group->members snapshot so a
 * new client can render every group's chips at startup. Map order
 * is unspecified.
 * @returns {$CancellablePromise<{ [_ in string]?: string[] }>}
 */
export function BroadcastListGroups() {
    return $Call.ByID(21264684).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType11($result);
    }));
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastRemove(sessionID) {
    return $Call.ByID(923790988, sessionID);
}

/**
 * BroadcastRemoveFrom drops a session from the named group. The
 * group itself is left in place even when it goes empty so the user
 * can re-add without re-creating it; explicit deletion uses
 * BroadcastGroupDelete.
 * @param {string} groupID
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastRemoveFrom(groupID, sessionID) {
    return $Call.ByID(1878133364, groupID, sessionID);
}

/**
 * BroadcastSetAll replaces the entire default-group membership in
 * one call. Useful for the manager's select-all / invert actions so
 * we don't emit N events.
 * @param {string[]} sessionIDs
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastSetAll(sessionIDs) {
    return $Call.ByID(2991512047, sessionIDs);
}

/**
 * BroadcastSetAllInGroup replaces membership of one named group.
 * @param {string} groupID
 * @param {string[]} sessionIDs
 * @returns {$CancellablePromise<void>}
 */
export function BroadcastSetAllInGroup(groupID, sessionIDs) {
    return $Call.ByID(3451020711, groupID, sessionIDs);
}

/**
 * CheckForUpdate resolves the newest release and compares against
 * the build-time injected version. Primary source is GitHub
 * Releases; on any error there it falls back to the legacy release
 * server's /api/latest. A user-set `update_check_base_url` skips
 * GitHub entirely. Honours the `update_check_disabled` setting
 * (default false = check enabled). Network/server failures return a
 * result with Error set rather than a Go error so the frontend
 * doesn't have to special-case them - the UI can fall silent if
 * Error != "" and IsNewer == false.
 * @returns {$CancellablePromise<$models.UpdateCheckResult>}
 */
export function CheckForUpdate() {
    return $Call.ByID(2347956003).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType12($result);
    }));
}

/**
 * ClearConnectionPassword removes the per-connection password from the vault
 * and clears the vault-key reference.
 * @param {string} connectionID
 * @returns {$CancellablePromise<void>}
 */
export function ClearConnectionPassword(connectionID) {
    return $Call.ByID(2499744755, connectionID);
}

/**
 * ClearConnectionVncPassword removes the stored VNC password.
 * @param {string} connectionID
 * @returns {$CancellablePromise<void>}
 */
export function ClearConnectionVncPassword(connectionID) {
    return $Call.ByID(3440490162, connectionID);
}

/**
 * ClipboardGetText reads the OS clipboard via the native Wails clipboard.
 * The webview's navigator.clipboard.readText() is blocked over a canvas
 * (no secure context / user-gesture path), so the VNC console reads the
 * clipboard through this instead to paste into the remote.
 * @returns {$CancellablePromise<string>}
 */
export function ClipboardGetText() {
    return $Call.ByID(3132142092);
}

/**
 * ClipboardSetText writes text to the OS clipboard via Wails. The VNC
 * console uses it to mirror the remote's RFB cut-text (copy from the
 * remote desktop) into the local clipboard.
 * @param {string} text
 * @returns {$CancellablePromise<boolean>}
 */
export function ClipboardSetText(text) {
    return $Call.ByID(1032748056, text);
}

/**
 * ConfirmQuit is called by the frontend after the user accepts the
 * "active sessions will be disconnected" prompt. Sets the flag so the
 * next WindowClosing event proceeds, then asks the app to quit.
 * @returns {$CancellablePromise<void>}
 */
export function ConfirmQuit() {
    return $Call.ByID(2432217600);
}

/**
 * @param {string} connectionID
 * @returns {$CancellablePromise<$models.ConnectionCopyInfo | null>}
 */
export function ConnectionCopyInfo(connectionID) {
    return $Call.ByID(3816831764, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType14($result);
    }));
}

/**
 * ConnectionRevealPassword resolves a connection -> credential -> vault
 * and returns the plaintext password for the resolved credential. Only
 * works on kind=password creds; other kinds return an error so the UI
 * can disable the button accordingly.
 * 
 * Use cases: pasting a sudo password into an open terminal without
 * flipping to the credentials tab.
 * @param {string} connectionID
 * @returns {$CancellablePromise<string>}
 */
export function ConnectionRevealPassword(connectionID) {
    return $Call.ByID(1557389389, connectionID);
}

/**
 * @param {$models.ConnectionsBatchUpdateInput} $in
 * @returns {$CancellablePromise<$models.ConnectionsBatchUpdateResult | null>}
 */
export function ConnectionsBatchUpdate($in) {
    return $Call.ByID(213302747, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType16($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.Connection | null>}
 */
export function ConnectionsClone(id) {
    return $Call.ByID(3943620147, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * @param {$models.ConnectionsCreateInput} $in
 * @returns {$CancellablePromise<store$0.Connection | null>}
 */
export function ConnectionsCreate($in) {
    return $Call.ByID(2927064594, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function ConnectionsDelete(id) {
    return $Call.ByID(2345448209, id);
}

/**
 * ConnectionsFavorites returns every connection flagged favorite.
 * @returns {$CancellablePromise<store$0.Connection[]>}
 */
export function ConnectionsFavorites() {
    return $Call.ByID(4177648875).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType19($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.Connection | null>}
 */
export function ConnectionsGet(id) {
    return $Call.ByID(3204093906, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * @param {string | null} folderID
 * @returns {$CancellablePromise<store$0.Connection[]>}
 */
export function ConnectionsList(folderID) {
    return $Call.ByID(1558209282, folderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType19($result);
    }));
}

/**
 * ConnectionsRecent returns the N most-recently connected entries.
 * Sidebar mounts these above the tree under "Recent". N comes from
 * settings (recent_connections_count) and defaults to 5.
 * @returns {$CancellablePromise<store$0.Connection[]>}
 */
export function ConnectionsRecent() {
    return $Call.ByID(873599885).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType19($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.ResolvedSettings | null>}
 */
export function ConnectionsResolve(id) {
    return $Call.ByID(837885766, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType21($result);
    }));
}

/**
 * ConnectionsSetFavorite toggles the favorite flag without touching
 * any other field on the connection (UpdateConnection is heavier).
 * @param {string} id
 * @param {boolean} fav
 * @returns {$CancellablePromise<void>}
 */
export function ConnectionsSetFavorite(id, fav) {
    return $Call.ByID(282213586, id, fav);
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function ConnectionsTouch(id) {
    return $Call.ByID(3667470729, id);
}

/**
 * @param {$models.ConnectionsUpdateInput} $in
 * @returns {$CancellablePromise<store$0.Connection | null>}
 */
export function ConnectionsUpdate($in) {
    return $Call.ByID(157328959, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * ConvertDynamicFolderToStatic snapshots every cached dynamic entry
 * into a real connection inside the same folder, drops the
 * dynamic_folders side-table row, and clears the cached entries. The
 * base folders row stays in place so connection inheritance for the
 * new rows works as expected. Operation is irreversible from the UI.
 * @param {string} folderID
 * @returns {$CancellablePromise<number>}
 */
export function ConvertDynamicFolderToStatic(folderID) {
    return $Call.ByID(2359957526, folderID);
}

/**
 * @param {string} name
 * @param {string | null} parentID
 * @returns {$CancellablePromise<store$0.CredentialFolder | null>}
 */
export function CredentialFoldersCreate(name, parentID) {
    return $Call.ByID(1013449597, name, parentID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType23($result);
    }));
}

/**
 * CredentialFoldersDelete removes the folder AND everything inside it:
 * subfolders via the FK cascade, credentials explicitly through
 * credSvc.Delete so vault secrets and sealed history follow the normal
 * deletion path. The credential_refs FK is ON DELETE SET NULL - left
 * alone it silently dumped the folder's credentials flat at the root,
 * which matched neither the connections tree nor user expectation.
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function CredentialFoldersDelete(id) {
    return $Call.ByID(4210166122, id);
}

/**
 * @returns {$CancellablePromise<store$0.CredentialFolder[]>}
 */
export function CredentialFoldersList() {
    return $Call.ByID(2994241185).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType24($result);
    }));
}

/**
 * @param {string} id
 * @param {string | null} name
 * @param {string | null} parentID
 * @param {boolean} clearParent
 * @returns {$CancellablePromise<store$0.CredentialFolder | null>}
 */
export function CredentialFoldersUpdate(id, name, parentID, clearParent) {
    return $Call.ByID(293416340, id, name, parentID, clearParent).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType23($result);
    }));
}

/**
 * @param {creds$0.CreateInput} $in
 * @returns {$CancellablePromise<creds$0.CreateResult | null>}
 */
export function CredentialsCreate($in) {
    return $Call.ByID(1585776553, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType26($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function CredentialsDelete(id) {
    return $Call.ByID(4195141342, id);
}

/**
 * CredentialsDeleteSecretHistory drops one history snapshot from both
 * the DB and the vault. No-op-safe: missing IDs return an error
 * without state changes.
 * @param {string} historyID
 * @returns {$CancellablePromise<void>}
 */
export function CredentialsDeleteSecretHistory(historyID) {
    return $Call.ByID(2022277950, historyID);
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.CredentialRef | null>}
 */
export function CredentialsGet(id) {
    return $Call.ByID(3107227931, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType28($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.CredentialHistoryEntry[]>}
 */
export function CredentialsHistory(id) {
    return $Call.ByID(1360057131, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType30($result);
    }));
}

/**
 * @returns {$CancellablePromise<store$0.CredentialRef[]>}
 */
export function CredentialsList() {
    return $Call.ByID(2783739749).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType31($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<string>}
 */
export function CredentialsRevealSecret(id) {
    return $Call.ByID(1459134760, id);
}

/**
 * CredentialsRevealSecretHistory unseals one history snapshot. UI
 * drives the 30s clipboard auto-clear the same way it does for
 * CredentialsRevealSecret. We log a "history revealed" audit event
 * so a reveal that produces a real plaintext is auditable.
 * @param {string} historyID
 * @returns {$CancellablePromise<string>}
 */
export function CredentialsRevealSecretHistory(historyID) {
    return $Call.ByID(3925935824, historyID);
}

/**
 * @param {$models.CredentialsRotateAPITokenInput} $in
 * @returns {$CancellablePromise<store$0.CredentialRef | null>}
 */
export function CredentialsRotateAPIToken($in) {
    return $Call.ByID(1312411311, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType28($result);
    }));
}

/**
 * @param {$models.CredentialsRotateKeyInput} $in
 * @returns {$CancellablePromise<store$0.CredentialRef | null>}
 */
export function CredentialsRotateKey($in) {
    return $Call.ByID(108739669, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType28($result);
    }));
}

/**
 * @param {string} id
 * @param {string} newPassword
 * @returns {$CancellablePromise<store$0.CredentialRef | null>}
 */
export function CredentialsRotatePassword(id, newPassword) {
    return $Call.ByID(4221844723, id, newPassword).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType28($result);
    }));
}

/**
 * CredentialsSecretHistory lists sealed previous-value snapshots for a
 * credential. Metadata only - call CredentialsRevealSecretHistory to
 * unseal one.
 * @param {string} id
 * @returns {$CancellablePromise<store$0.CredentialSecretHistoryEntry[]>}
 */
export function CredentialsSecretHistory(id) {
    return $Call.ByID(1096287207, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType33($result);
    }));
}

/**
 * @param {$models.CredentialsUpdateInput} $in
 * @returns {$CancellablePromise<store$0.CredentialRef | null>}
 */
export function CredentialsUpdate($in) {
    return $Call.ByID(427641400, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType28($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.UsageRef[]>}
 */
export function CredentialsUsage(id) {
    return $Call.ByID(1714078284, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType35($result);
    }));
}

/**
 * DownloadUpdate streams the asset captured by the last CheckForUpdate
 * into a staging slot next to the running binary, verifying its sha256
 * against the manifest value before any swap. It deliberately takes no
 * URL parameter - the backend only downloads what it derived from its
 * own update check, never what the webview asks for. On Unix the swap
 * happens during Download itself (renames are safe over a running
 * binary). On Windows the swap is deferred to an apply script that
 * ApplyUpdate spawns just before the app exits.
 * @returns {$CancellablePromise<updater$0.DownloadResult | null>}
 */
export function DownloadUpdate() {
    return $Call.ByID(115027584).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType37($result);
    }));
}

/**
 * @param {string} folderID
 * @returns {$CancellablePromise<store$0.DynamicEntry[]>}
 */
export function DynamicEntriesList(folderID) {
    return $Call.ByID(1307885234, folderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType39($result);
    }));
}

/**
 * @param {$models.DynamicFolderCreateInput} $in
 * @returns {$CancellablePromise<store$0.Folder | null>}
 */
export function DynamicFolderCreate($in) {
    return $Call.ByID(906502258, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType41($result);
    }));
}

/**
 * @param {string} folderID
 * @returns {$CancellablePromise<store$0.DynamicFolder | null>}
 */
export function DynamicFolderGet(folderID) {
    return $Call.ByID(2578538674, folderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType43($result);
    }));
}

/**
 * @param {string} folderID
 * @returns {$CancellablePromise<void>}
 */
export function DynamicFolderRefreshNow(folderID) {
    return $Call.ByID(1473028095, folderID);
}

/**
 * @param {$models.DynamicFolderUpdateInput} $in
 * @returns {$CancellablePromise<void>}
 */
export function DynamicFolderUpdate($in) {
    return $Call.ByID(943367839, $in);
}

/**
 * @returns {$CancellablePromise<store$0.DynamicFolder[]>}
 */
export function DynamicFoldersList() {
    return $Call.ByID(2814506823).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType44($result);
    }));
}

/**
 * ExplorerMenuRegister adds "Open in ssh-tool" to the OS file
 * manager's right-click menu for directories (Explorer on Windows,
 * Dolphin/Nautilus on Linux). Per-user / no admin. Idempotent.
 * @returns {$CancellablePromise<void>}
 */
export function ExplorerMenuRegister() {
    return $Call.ByID(2441846592);
}

/**
 * ExplorerMenuStatus returns a short description of the installed
 * integration ("" = not installed).
 * @returns {$CancellablePromise<string>}
 */
export function ExplorerMenuStatus() {
    return $Call.ByID(575179119);
}

/**
 * ExplorerMenuUnregister removes the file-manager integration.
 * @returns {$CancellablePromise<void>}
 */
export function ExplorerMenuUnregister() {
    return $Call.ByID(3804843843);
}

/**
 * @param {$models.ExportSubtreeRequest} req
 * @returns {$CancellablePromise<$models.ExportSubtreeResult | null>}
 */
export function ExportSubtree(req) {
    return $Call.ByID(2615218739, req).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType46($result);
    }));
}

/**
 * FetchArchiveURL retrieves an archive payload from a remote URL
 * (typically the ssh-tool-catalog /api/bundle endpoint). Returns
 * the raw text so the frontend can hand it to ImportArchive with
 * the user-picked conflict mode + dry-run flag.
 * 
 * Done server-side so the request inherits ssh-tool's network +
 * any future proxy/cert config, and so the user doesn't bump into
 * CORS in the WebView for cross-origin reads.
 * @param {string} rawURL
 * @returns {$CancellablePromise<string>}
 */
export function FetchArchiveURL(rawURL) {
    return $Call.ByID(1937629284, rawURL);
}

/**
 * FetchReleaseNotes pulls the markdown changelog for `version` from
 * the release server. Used by the in-app update modal to render
 * release notes inline. Network errors and 404s come back as a
 * populated ErrorMsg with the other fields blank.
 * @param {string} version
 * @returns {$CancellablePromise<$models.ReleaseNotes>}
 */
export function FetchReleaseNotes(version) {
    return $Call.ByID(835582245, version).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType47($result);
    }));
}

/**
 * @param {$models.FoldersCreateInput} $in
 * @returns {$CancellablePromise<store$0.Folder | null>}
 */
export function FoldersCreate($in) {
    return $Call.ByID(3035981430, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType41($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function FoldersDelete(id) {
    return $Call.ByID(3274317949, id);
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<store$0.Folder | null>}
 */
export function FoldersGet(id) {
    return $Call.ByID(1055059758, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType41($result);
    }));
}

/**
 * @returns {$CancellablePromise<store$0.Folder[]>}
 */
export function FoldersList() {
    return $Call.ByID(2539786558).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType48($result);
    }));
}

/**
 * @param {$models.FoldersUpdateInput} $in
 * @returns {$CancellablePromise<store$0.Folder | null>}
 */
export function FoldersUpdate($in) {
    return $Call.ByID(2482282907, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType41($result);
    }));
}

/**
 * ForwardsActive returns running forwards (optionally filtered by session).
 * @param {string} sessionID
 * @returns {$CancellablePromise<ssh$0.ForwardStatus[]>}
 */
export function ForwardsActive(sessionID) {
    return $Call.ByID(3377199405, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType50($result);
    }));
}

/**
 * @param {$models.ForwardCreateInput} $in
 * @returns {$CancellablePromise<store$0.PortForward | null>}
 */
export function ForwardsCreate($in) {
    return $Call.ByID(169157747, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType52($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function ForwardsDelete(id) {
    return $Call.ByID(2180394304, id);
}

/**
 * @param {string} connectionID
 * @returns {$CancellablePromise<store$0.PortForward[]>}
 */
export function ForwardsList(connectionID) {
    return $Call.ByID(601298219, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType53($result);
    }));
}

/**
 * ForwardsListAll returns every port-forward across all connections.
 * The global quick palette uses this to build its tunnel / bookmark
 * rows in a single IPC instead of fanning out per-connection.
 * @returns {$CancellablePromise<store$0.PortForward[]>}
 */
export function ForwardsListAll() {
    return $Call.ByID(1758171822).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType53($result);
    }));
}

/**
 * @param {string} forwardID
 * @param {store$0.ProxyBookmark[]} bookmarks
 * @returns {$CancellablePromise<void>}
 */
export function ForwardsSetBookmarks(forwardID, bookmarks) {
    return $Call.ByID(3861167066, forwardID, bookmarks);
}

/**
 * ForwardsStart instantiates a persisted forward against an active session.
 * @param {string} forwardID
 * @param {string} sessionID
 * @returns {$CancellablePromise<ssh$0.ForwardStatus | null>}
 */
export function ForwardsStart(forwardID, sessionID) {
    return $Call.ByID(720013585, forwardID, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType54($result);
    }));
}

/**
 * ForwardsStop tears down a running forward by spec id.
 * @param {string} forwardID
 * @returns {$CancellablePromise<void>}
 */
export function ForwardsStop(forwardID) {
    return $Call.ByID(1504544203, forwardID);
}

/**
 * @param {$models.ForwardUpdateInput} $in
 * @returns {$CancellablePromise<store$0.PortForward | null>}
 */
export function ForwardsUpdate($in) {
    return $Call.ByID(2050473026, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType52($result);
    }));
}

/**
 * FrontendLog writes a line from the frontend into the same log sink the
 * in-app Log viewer reads (the Go logger). Lets the webview surface diag
 * output to a user who can't open the browser console - e.g. someone
 * running the app on a remote desktop. Prefixed so frontend lines are
 * distinguishable from backend ones.
 * @param {string} line
 * @returns {$CancellablePromise<void>}
 */
export function FrontendLog(line) {
    return $Call.ByID(956996769, line);
}

/**
 * GetConnectionHasPassword returns true if a per-connection password is stored.
 * @param {string} connectionID
 * @returns {$CancellablePromise<boolean>}
 */
export function GetConnectionHasPassword(connectionID) {
    return $Call.ByID(1080161688, connectionID);
}

/**
 * GetConnectionHasVncPassword reports whether a VNC password is stored.
 * @param {string} connectionID
 * @returns {$CancellablePromise<boolean>}
 */
export function GetConnectionHasVncPassword(connectionID) {
    return $Call.ByID(2822315999, connectionID);
}

/**
 * HideToTray hides the main window without quitting. Used by the
 * frontend "Minimize to tray" button.
 * @returns {$CancellablePromise<void>}
 */
export function HideToTray() {
    return $Call.ByID(2285725636);
}

/**
 * HttpDo issues a one-shot HTTP request. Routes through SocksAddr if
 * set - typically the local bind of an active dynamic forward
 * ("127.0.0.1:1080" etc.) - so requests can reach endpoints inside
 * the remote network without curl gymnastics. Body is sent verbatim;
 * the caller decides Content-Type via the headers list. Response
 * body is capped at 4 MiB; the rest is dropped with truncated=true.
 * @param {httpc$0.Request} req
 * @returns {$CancellablePromise<httpc$0.Response | null>}
 */
export function HttpDo(req) {
    return $Call.ByID(1818163068, req).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType56($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<$models.ImagePayload | null>}
 */
export function ImagesGet(id) {
    return $Call.ByID(628051325, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType58($result);
    }));
}

/**
 * ImagesList returns metadata for every image already in the DB so
 * the IconPicker can offer "choose from existing" alongside upload.
 * Bytes are NOT included - the frontend hydrates each thumbnail
 * lazily via ImagesGet through the existing imageCache.
 * @returns {$CancellablePromise<store$0.ImageSummary[]>}
 */
export function ImagesList() {
    return $Call.ByID(2756601723).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType60($result);
    }));
}

/**
 * ImagesSetConnection assigns an image to a connection. Empty clears.
 * @param {string} connID
 * @param {string} imageID
 * @returns {$CancellablePromise<void>}
 */
export function ImagesSetConnection(connID, imageID) {
    return $Call.ByID(160931501, connID, imageID);
}

/**
 * ImagesSetCredential assigns an image to a credential. Empty clears.
 * @param {string} credID
 * @param {string} imageID
 * @returns {$CancellablePromise<void>}
 */
export function ImagesSetCredential(credID, imageID) {
    return $Call.ByID(3312600100, credID, imageID);
}

/**
 * ImagesSetFolder assigns an image to a folder. Pass empty imageID to
 * clear the icon (folder reverts to the default emoji).
 * @param {string} folderID
 * @param {string} imageID
 * @returns {$CancellablePromise<void>}
 */
export function ImagesSetFolder(folderID, imageID) {
    return $Call.ByID(3526973265, folderID, imageID);
}

/**
 * ImagesUpload stores a base64-encoded image and returns the image id.
 * MIME is required so the frontend can render it back as a data URI
 * later. Dedup happens inside store.PutImage (MD5 content addressing).
 * @param {string} b64Data
 * @param {string} mime
 * @returns {$CancellablePromise<string>}
 */
export function ImagesUpload(b64Data, mime) {
    return $Call.ByID(120458694, b64Data, mime);
}

/**
 * @param {$models.ImportArchiveRequest} req
 * @returns {$CancellablePromise<exporter$0.ImportSummary | null>}
 */
export function ImportArchive(req) {
    return $Call.ByID(3382034674, req).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType62($result);
    }));
}

/**
 * LaunchExternalTerminal spawns the requested OS terminal with an
 * `ssh ...` command pointed at the resolved settings of the given
 * connection. Returns once the spawn is dispatched (the terminal
 * runs detached from the app).
 * 
 * kind: "powershell" | "cmd" | "windowsterminal" (Windows only for
 * now). On a non-Windows host returns an error so the caller can
 * show a helpful message.
 * @param {string} connectionID
 * @param {string} kind
 * @returns {$CancellablePromise<void>}
 */
export function LaunchExternalTerminal(connectionID, kind) {
    return $Call.ByID(3160687673, connectionID, kind);
}

/**
 * LoadTextFile pops a native Open dialog and returns the chosen
 * file's contents. Used by the import-archive flow so the user
 * doesn't have to copy-paste an archive into the textarea. Refuses
 * files larger than 32 MiB to keep a stray binary pick from
 * freezing the renderer.
 * @param {string} title
 * @returns {$CancellablePromise<$models.LoadTextFileResult | null>}
 */
export function LoadTextFile(title) {
    return $Call.ByID(2336303364, title).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType64($result);
    }));
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function LocalShellDisconnect(sessionID) {
    return $Call.ByID(2774099108, sessionID);
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<$models.ScrollbackSnapshot>}
 */
export function LocalShellGetScrollback(sessionID) {
    return $Call.ByID(4083141096, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType65($result);
    }));
}

/**
 * @returns {$CancellablePromise<$models.LocalShellInfo[]>}
 */
export function LocalShellList() {
    return $Call.ByID(401932158).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType67($result);
    }));
}

/**
 * LocalShellOpen spawns a new local PTY session. kind is one of the
 * platform-supported shells; "" or "auto" picks a sensible default
 * (see internal/local.resolveShell). Output events use the same
 * pty_output:<sessionID> channel as SSH so the xterm component
 * doesn't need to know which kind it is.
 * @param {string} kind
 * @param {string} dir
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<$models.LocalShellOpenResult | null>}
 */
export function LocalShellOpen(kind, dir, cols, rows) {
    return $Call.ByID(2863600362, kind, dir, cols, rows).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType69($result);
    }));
}

/**
 * @param {string} sessionID
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<void>}
 */
export function LocalShellResize(sessionID, cols, rows) {
    return $Call.ByID(1685717574, sessionID, cols, rows);
}

/**
 * @param {string} sessionID
 * @param {string} dataB64
 * @returns {$CancellablePromise<void>}
 */
export function LocalShellWrite(sessionID, dataB64) {
    return $Call.ByID(1958170285, sessionID, dataB64);
}

/**
 * LogDir returns the path to the rotating log file directory so the
 * Settings page can show + open it.
 * @returns {$CancellablePromise<string>}
 */
export function LogDir() {
    return $Call.ByID(3860810616);
}

/**
 * MobaXtermImport parses a MobaXterm .mxtsessions export and creates
 * one connection per SSH session, rebuilding the bookmark folder tree
 * under rootFolderID ("" = DB root). Passwords aren't in the export;
 * non-SSH session types (RDP, telnet, ...) are counted and skipped.
 * @param {string} text
 * @param {string} rootFolderID
 * @returns {$CancellablePromise<mobaxterm$0.Summary | null>}
 */
export function MobaXtermImport(text, rootFolderID) {
    return $Call.ByID(753160963, text, rootFolderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType71($result);
    }));
}

/**
 * NetworkProfileCreate parses a wg-quick conf, stores its secrets in
 * the vault and the rest in the DB.
 * @param {string} name
 * @param {string} confText
 * @returns {$CancellablePromise<$models.NetworkProfileInfo | null>}
 */
export function NetworkProfileCreate(name, confText) {
    return $Call.ByID(4293406034, name, confText).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType73($result);
    }));
}

/**
 * NetworkProfileDelete stops the tunnel and removes the row + vault
 * secrets. Connections still referencing the id fail to connect with
 * "not found" - visible, not silent.
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function NetworkProfileDelete(id) {
    return $Call.ByID(2223320401, id);
}

/**
 * NetworkProfileRenderConf renders the stored profile back to
 * wg-quick text for the edit form. Secrets come out as the
 * wg.KeepSecret placeholder; NetworkProfileUpdate translates that
 * back to "keep the vault value".
 * @param {string} id
 * @returns {$CancellablePromise<string>}
 */
export function NetworkProfileRenderConf(id) {
    return $Call.ByID(2254941776, id);
}

/**
 * NetworkProfileSetPolicy updates the connect policy: mode is
 * "always" or "auto"; paused is the per-profile kill switch (dial
 * direct, don't start the tunnel). Pausing also stops a running
 * tunnel so the switch takes effect immediately.
 * @param {string} id
 * @param {string} mode
 * @param {boolean} paused
 * @returns {$CancellablePromise<$models.NetworkProfileInfo | null>}
 */
export function NetworkProfileSetPolicy(id, mode, paused) {
    return $Call.ByID(783029456, id, mode, paused).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType73($result);
    }));
}

/**
 * NetworkProfileStop tears down a running tunnel (connections dialed
 * through it drop, like killing a session).
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function NetworkProfileStop(id) {
    return $Call.ByID(2038703486, id);
}

/**
 * NetworkProfileTest brings the tunnel up (vault + device) and
 * reports its status - a cheap "does this config load" check. It does
 * not verify the peer answers (WireGuard is silent until traffic
 * flows); LastHandshake stays 0 until the first real dial.
 * @param {string} id
 * @returns {$CancellablePromise<wg$0.Status | null>}
 */
export function NetworkProfileTest(id) {
    return $Call.ByID(3728857406, id).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType75($result);
    }));
}

/**
 * NetworkProfileUpdate renames a profile and, when confText is
 * non-empty, replaces its whole configuration (and vault secrets).
 * A running tunnel is stopped so the next connect uses the new config.
 * @param {string} id
 * @param {string} name
 * @param {string} confText
 * @returns {$CancellablePromise<$models.NetworkProfileInfo | null>}
 */
export function NetworkProfileUpdate(id, name, confText) {
    return $Call.ByID(1523773567, id, name, confText).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType73($result);
    }));
}

/**
 * NetworkProfilesList returns every stored profile with live status.
 * @returns {$CancellablePromise<$models.NetworkProfileInfo[]>}
 */
export function NetworkProfilesList() {
    return $Call.ByID(983369255).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType76($result);
    }));
}

/**
 * OpenNativeTerminal spawns the user's preferred OS terminal with
 * nothing attached - fresh shell, no SSH, no command. Useful for
 * running a quick local command without leaving the app.
 * 
 * kind on Windows: "powershell" | "cmd" | "windowsterminal".
 * On Linux: ignored - uses $TERMINAL, then x-terminal-emulator,
 * then a small fallback list (gnome-terminal, konsole, xterm).
 * On macOS: ignored - opens Terminal.app via `open -a Terminal`.
 * @param {string} kind
 * @returns {$CancellablePromise<void>}
 */
export function OpenNativeTerminal(kind) {
    return $Call.ByID(3013851190, kind);
}

/**
 * OpenURL routes a URL to the system browser via Wails runtime. Used by
 * the xterm web-links addon when the user clicks a URL in the terminal.
 * @param {string} url
 * @returns {$CancellablePromise<void>}
 */
export function OpenURL(url) {
    return $Call.ByID(3584934946, url);
}

/**
 * OpksshCertStatus reports when the opkssh credential's cert was
 * issued and when the connect path will force a browser re-login, so
 * the editor can render "re-login in ~6d23h" instead of raw config
 * numbers. Read-only; never triggers a refresh.
 * @param {string} credentialID
 * @returns {$CancellablePromise<$models.OpksshCertStatusResult | null>}
 */
export function OpksshCertStatus(credentialID) {
    return $Call.ByID(2850976259, credentialID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType78($result);
    }));
}

/**
 * PathIsDir reports whether path is a directory. Used by the SFTP
 * drag-and-drop upload flow - when the OS drops file paths into a
 * pane, we need to know whether to call SftpStartUpload (file) or
 * SftpStartUploadDir (directory) per entry.
 * @param {string} path
 * @returns {$CancellablePromise<boolean>}
 */
export function PathIsDir(path) {
    return $Call.ByID(3493822441, path);
}

/**
 * PickAnsibleInventoryFile opens a native Open File dialog so the
 * dynamic-folder editor doesn't make the user copy the inventory
 * path manually. No extension filter - the v3 dialog shim only
 * carries Title for now; the parser dispatches on extension at
 * read time anyway, so picking the wrong file just fails loudly.
 * @returns {$CancellablePromise<string>}
 */
export function PickAnsibleInventoryFile() {
    return $Call.ByID(2644825518);
}

/**
 * PinDynamicEntry promotes a single dynamic entry into a real connection.
 * Reads the dynamic_entry, lifts provider-specific vars (Ansible) into
 * the new connection's overrides, records the pin so the next refresh
 * skips this external_id. Returns the created connection.
 * @param {$models.PinDynamicEntryInput} $in
 * @returns {$CancellablePromise<store$0.Connection | null>}
 */
export function PinDynamicEntry($in) {
    return $Call.ByID(728029883, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType18($result);
    }));
}

/**
 * Ping smoke command.
 * @param {string} name
 * @returns {$CancellablePromise<string>}
 */
export function Ping(name) {
    return $Call.ByID(1003072145, name);
}

/**
 * ProfileStats counts connections (total + resolved VNC-enabled),
 * folders (total + dynamic), configured port forwards + their proxy
 * bookmarks, credentials, and cached dynamic-inventory entries
 * bucketed by kind (hosts / VMs / LXC / cloud servers).
 * @returns {$CancellablePromise<$models.ProfileStats | null>}
 */
export function ProfileStats() {
    return $Call.ByID(3283721229).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType80($result);
    }));
}

/**
 * PuttyRegImport parses a PuTTY registry export (.reg, also KiTTY)
 * and creates one connection per Protocol=ssh session, flat under
 * rootFolderID. PuTTY stores no passwords, so nothing is lost.
 * @param {string} text
 * @param {string} rootFolderID
 * @returns {$CancellablePromise<puttyreg$0.Summary | null>}
 */
export function PuttyRegImport(text, rootFolderID) {
    return $Call.ByID(3401251516, text, rootFolderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType82($result);
    }));
}

/**
 * RdmImport parses a Devolutions RDM JSON export and merges it into the
 * store. Frontend uploads the raw JSON string; we return a summary so the
 * UI can show what landed and what needs follow-up.
 * RdmImport parses a Devolutions RDM JSON export and merges it into the store.
 * rootFolderID optionally places all imported top-level folders under an
 * existing connection-tree folder; empty string means the DB root.
 * @param {string} jsonText
 * @param {string} rootFolderID
 * @returns {$CancellablePromise<rdm$0.Summary | null>}
 */
export function RdmImport(jsonText, rootFolderID) {
    return $Call.ByID(3457719183, jsonText, rootFolderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType84($result);
    }));
}

/**
 * RecordingActive snapshots every live recording so a reloaded /
 * detached window can restore its indicators.
 * @returns {$CancellablePromise<$models.RecordingState[]>}
 */
export function RecordingActive() {
    return $Call.ByID(2560532950).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType86($result);
    }));
}

/**
 * RecordingDelete removes a .cast file. Refuses while the file is
 * still being written by a live recording.
 * @param {string} path
 * @returns {$CancellablePromise<void>}
 */
export function RecordingDelete(path) {
    return $Call.ByID(3246603391, path);
}

/**
 * RecordingRead returns the raw asciicast text for the in-app player.
 * Capped: a recording bigger than the cap should be played with an
 * external tool rather than shipped through the IPC bridge in one
 * string.
 * @param {string} path
 * @returns {$CancellablePromise<string>}
 */
export function RecordingRead(path) {
    return $Call.ByID(4126225608, path);
}

/**
 * RecordingStart begins capturing the session's PTY output to a new
 * asciicast v2 file in RecordingsDir. Output-only: keystrokes are
 * never written, so typed secrets can't end up in the file.
 * @param {string} sessionID
 * @returns {$CancellablePromise<$models.RecordingState | null>}
 */
export function RecordingStart(sessionID) {
    return $Call.ByID(185130520, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType87($result);
    }));
}

/**
 * RecordingStop finalises the session's recording and returns where
 * the file landed.
 * @param {string} sessionID
 * @returns {$CancellablePromise<$models.RecordingState | null>}
 */
export function RecordingStop(sessionID) {
    return $Call.ByID(1444310460, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType87($result);
    }));
}

/**
 * RecordingsDir returns where new .cast files land. Overridable via
 * the "recordings_dir" setting; default <DataDir>/recordings.
 * @returns {$CancellablePromise<string>}
 */
export function RecordingsDir() {
    return $Call.ByID(3048300372);
}

/**
 * RecordingsList returns every .cast file in the recordings folder,
 * newest first, with parsed header + duration for the browser UI.
 * @returns {$CancellablePromise<recorder$0.FileInfo[]>}
 */
export function RecordingsList() {
    return $Call.ByID(103046209).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType89($result);
    }));
}

/**
 * RecordingsOpenDir reveals the recordings folder in the OS file
 * manager. Creates it first so the button works before the first
 * recording ever lands. Not BrowserOpenURL("file://...") - file URLs
 * with Windows backslash paths are unreliable through ShellExecute.
 * @returns {$CancellablePromise<void>}
 */
export function RecordingsOpenDir() {
    return $Call.ByID(2624936600);
}

/**
 * RecordingsPickDir opens a native directory picker for the Settings
 * "recordings folder" field. Empty result = cancelled.
 * @returns {$CancellablePromise<string>}
 */
export function RecordingsPickDir() {
    return $Call.ByID(2722966415);
}

/**
 * RegisterURLScheme writes the OS-level registration that binds
 * `ssh-tool://` URIs to this executable. Per-user / no admin.
 * Idempotent. See url_scheme_*.go for platform specifics.
 * @returns {$CancellablePromise<void>}
 */
export function RegisterURLScheme() {
    return $Call.ByID(749737630);
}

/**
 * SaveTextFile pops a native Save dialog and writes content there.
 * Used by the connection-export flow so the user can drop a TOML
 * archive on disk without leaving the app. Empty path returned =
 * user cancelled. Returns the actual path written on success.
 * @param {string} suggestedName
 * @param {string} content
 * @returns {$CancellablePromise<string>}
 */
export function SaveTextFile(suggestedName, content) {
    return $Call.ByID(523683609, suggestedName, content);
}

/**
 * SetConnectionPassword stores a per-connection password in the vault and
 * records the vault-key reference on the connection row.
 * @param {string} connectionID
 * @param {string} password
 * @returns {$CancellablePromise<void>}
 */
export function SetConnectionPassword(connectionID, password) {
    return $Call.ByID(544548906, connectionID, password);
}

/**
 * SetConnectionVncPassword stores the RFB password for a connection in
 * the vault and records the key on the connection row.
 * @param {string} connectionID
 * @param {string} password
 * @returns {$CancellablePromise<void>}
 */
export function SetConnectionVncPassword(connectionID, password) {
    return $Call.ByID(3239149457, connectionID, password);
}

/**
 * SetWindowTitle updates the OS window (and taskbar) title. The
 * frontend calls this to reflect the active connection/section; Wails
 * v3 alpha doesn't propagate document.title to the native title on its
 * own. No-op when the window isn't wired yet (early startup).
 * @param {string} title
 * @returns {$CancellablePromise<void>}
 */
export function SetWindowTitle(title) {
    return $Call.ByID(2595010483, title);
}

/**
 * @param {string} key
 * @returns {$CancellablePromise<void>}
 */
export function SettingsDelete(key) {
    return $Call.ByID(2772647723, key);
}

/**
 * SettingsGet returns the value of an app-wide setting, or "" if unset.
 * @param {string} key
 * @returns {$CancellablePromise<string>}
 */
export function SettingsGet(key) {
    return $Call.ByID(3976832780, key);
}

/**
 * @param {string} key
 * @param {string} value
 * @returns {$CancellablePromise<void>}
 */
export function SettingsSet(key, value) {
    return $Call.ByID(1796722848, key, value);
}

/**
 * @param {string} transferID
 * @returns {$CancellablePromise<void>}
 */
export function SftpCancelTransfer(transferID) {
    return $Call.ByID(3922802881, transferID);
}

/**
 * @param {string} sessionID
 * @param {string} remotePath
 * @returns {$CancellablePromise<$models.SftpListResult | null>}
 */
export function SftpList(sessionID, remotePath) {
    return $Call.ByID(1007414552, sessionID, remotePath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType91($result);
    }));
}

/**
 * @param {string} sessionID
 * @param {string} remotePath
 * @returns {$CancellablePromise<void>}
 */
export function SftpMkdir(sessionID, remotePath) {
    return $Call.ByID(2723140891, sessionID, remotePath);
}

/**
 * SftpPickDownloadDest opens a native Save File dialog and returns the
 * chosen path. The frontend calls this before SftpDownload so it doesn't
 * have to know the local filesystem layout. Empty result = user
 * cancelled.
 * @param {string} suggestedName
 * @returns {$CancellablePromise<string>}
 */
export function SftpPickDownloadDest(suggestedName) {
    return $Call.ByID(783982981, suggestedName);
}

/**
 * SftpPickDownloadDirDest opens a native directory picker for the
 * "where should the downloaded folder land" question.
 * @returns {$CancellablePromise<string>}
 */
export function SftpPickDownloadDirDest() {
    return $Call.ByID(2521088368);
}

/**
 * SftpPickUploadDirSource opens a native directory picker. Returns
 * the chosen local directory path (empty on cancel). Used by the
 * recursive upload flow.
 * @returns {$CancellablePromise<string>}
 */
export function SftpPickUploadDirSource() {
    return $Call.ByID(2671751666);
}

/**
 * SftpPickUploadSource opens a native Open File dialog and returns the
 * chosen local path. Empty result = user cancelled.
 * @returns {$CancellablePromise<string>}
 */
export function SftpPickUploadSource() {
    return $Call.ByID(999030933);
}

/**
 * @param {string} sessionID
 * @param {string} remotePath
 * @param {number} maxBytes
 * @returns {$CancellablePromise<$models.SftpPreview | null>}
 */
export function SftpReadPreview(sessionID, remotePath, maxBytes) {
    return $Call.ByID(40448224, sessionID, remotePath, maxBytes).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType93($result);
    }));
}

/**
 * @param {string} sessionID
 * @param {string} remotePath
 * @returns {$CancellablePromise<void>}
 */
export function SftpRemove(sessionID, remotePath) {
    return $Call.ByID(2664696088, sessionID, remotePath);
}

/**
 * @param {string} sessionID
 * @param {string} oldPath
 * @param {string} newPath
 * @returns {$CancellablePromise<void>}
 */
export function SftpRename(sessionID, oldPath, newPath) {
    return $Call.ByID(3014038834, sessionID, oldPath, newPath);
}

/**
 * SftpStartDownload begins streaming a remote file to localPath, emitting
 * progress as "sftp_progress:<transferId>" events. Returns the
 * transferId so the frontend can correlate the events and call
 * SftpCancelTransfer if needed.
 * 
 * Returns once the transfer goroutine is spawned. The final
 * success/failure is signalled via the same progress channel with
 * Done=true (and Err set on failure).
 * @param {string} sessionID
 * @param {string} remotePath
 * @param {string} localPath
 * @returns {$CancellablePromise<string>}
 */
export function SftpStartDownload(sessionID, remotePath, localPath) {
    return $Call.ByID(1013413652, sessionID, remotePath, localPath);
}

/**
 * SftpStartDownloadDir mirrors a remote directory tree locally. The
 * frontend uses the same sftp_progress event stream as single-file
 * downloads; the progress payload carries DirProgress's files_done /
 * files_total / bytes_done / bytes_total plus the current relative
 * path so the UI can show "Downloading subdir/file.log".
 * @param {string} sessionID
 * @param {string} remoteRoot
 * @param {string} localRoot
 * @returns {$CancellablePromise<string>}
 */
export function SftpStartDownloadDir(sessionID, remoteRoot, localRoot) {
    return $Call.ByID(2143616011, sessionID, remoteRoot, localRoot);
}

/**
 * @param {string} sessionID
 * @param {string} localPath
 * @param {string} remotePath
 * @returns {$CancellablePromise<string>}
 */
export function SftpStartUpload(sessionID, localPath, remotePath) {
    return $Call.ByID(2683797613, sessionID, localPath, remotePath);
}

/**
 * SftpStartUploadDir is the upload mirror of SftpStartDownloadDir.
 * @param {string} sessionID
 * @param {string} localRoot
 * @param {string} remoteRoot
 * @returns {$CancellablePromise<string>}
 */
export function SftpStartUploadDir(sessionID, localRoot, remoteRoot) {
    return $Call.ByID(3620516956, sessionID, localRoot, remoteRoot);
}

/**
 * @param {string} sessionID
 * @param {string} remotePath
 * @returns {$CancellablePromise<ssh$0.SftpEntry | null>}
 */
export function SftpStat(sessionID, remotePath) {
    return $Call.ByID(3650982466, sessionID, remotePath).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType95($result);
    }));
}

/**
 * ShowFromTray restores the main window.
 * @returns {$CancellablePromise<void>}
 */
export function ShowFromTray() {
    return $Call.ByID(140041132);
}

/**
 * @param {store$0.SnippetInput} $in
 * @returns {$CancellablePromise<store$0.Snippet | null>}
 */
export function SnippetCreate($in) {
    return $Call.ByID(2046363730, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType97($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function SnippetDelete(id) {
    return $Call.ByID(4271245393, id);
}

/**
 * SnippetSendToSession writes the snippet body into the named session's
 * stdin (after a RecordSnippetUse bump). Trailing newline is appended
 * when missing so single-line snippets execute as the user would expect.
 * @param {string} snippetID
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function SnippetSendToSession(snippetID, sessionID) {
    return $Call.ByID(3786027447, snippetID, sessionID);
}

/**
 * @param {string} id
 * @param {store$0.SnippetInput} $in
 * @returns {$CancellablePromise<store$0.Snippet | null>}
 */
export function SnippetUpdate(id, $in) {
    return $Call.ByID(3571698559, id, $in).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType97($result);
    }));
}

/**
 * SnippetsList returns snippets visible for the optional connection.
 * When connectionID is "" only global snippets are returned; otherwise
 * both global + per-connection snippets are returned, ordered by recent
 * use first.
 * @param {string} connectionID
 * @returns {$CancellablePromise<store$0.Snippet[]>}
 */
export function SnippetsList(connectionID) {
    return $Call.ByID(285056807, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType98($result);
    }));
}

/**
 * SshActiveSessionCount returns how many live SSH sessions are in the
 * pool. Used by the quit-confirm path before deciding whether to prompt.
 * @returns {$CancellablePromise<number>}
 */
export function SshActiveSessionCount() {
    return $Call.ByID(2280864468);
}

/**
 * @returns {$CancellablePromise<$models.ActiveSessionInfo[]>}
 */
export function SshActiveSessions() {
    return $Call.ByID(3292933058).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType100($result);
    }));
}

/**
 * SshCancelConnect aborts an in-flight connect for connectionID (or a
 * dynamic entry key) that's blocked on opkssh OIDC login. Safe to call when
 * nothing is in flight - it's a no-op then. Returns true if a connect was
 * actually cancelled.
 * @param {string} key
 * @returns {$CancellablePromise<boolean>}
 */
export function SshCancelConnect(key) {
    return $Call.ByID(2031011963, key);
}

/**
 * SshCancelReconnect aborts a pending retry loop. No-op if there isn't
 * one running for this session id.
 * @param {string} oldSessionID
 * @returns {$CancellablePromise<void>}
 */
export function SshCancelReconnect(oldSessionID) {
    return $Call.ByID(1591367324, oldSessionID);
}

/**
 * SshConfigImport parses an OpenSSH client config and writes one
 * connection per non-wildcard Host block. rootFolderID groups the
 * imports under an existing folder (empty = DB root).
 * @param {string} text
 * @param {string} rootFolderID
 * @returns {$CancellablePromise<sshconfig$0.Summary | null>}
 */
export function SshConfigImport(text, rootFolderID) {
    return $Call.ByID(1631847250, text, rootFolderID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType102($result);
    }));
}

/**
 * @param {string} connectionID
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnect(connectionID) {
    return $Call.ByID(1360707181, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * SshConnectAdvanced lets the caller override credentialID, username,
 * and / or password for one attempt. Empty strings on each field
 * mean "use whatever resolution would normally pick"; a non-empty
 * value overrides. The three knobs are independent - e.g. override
 * just the username while keeping the persisted credential, or
 * supply a one-shot password without a credential. Returned session
 * uses the overridden values; persisted connection is unchanged.
 * @param {string} connectionID
 * @param {string} overrideCredentialID
 * @param {string} overrideUsername
 * @param {string} overridePassword
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnectAdvanced(connectionID, overrideCredentialID, overrideUsername, overridePassword) {
    return $Call.ByID(2119326771, connectionID, overrideCredentialID, overrideUsername, overridePassword).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * SshConnectDynamic spins up an SSH session against a cached
 * dynamic_entry. The entry is read-only and has no `connections` row,
 * so we construct a synthetic store.Connection in memory carrying the
 * hostname + folder lineage and feed it through the same resolver +
 * SSH layer the persistent-connection path uses.
 * @param {string} folderID
 * @param {string} entryID
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnectDynamic(folderID, entryID) {
    return $Call.ByID(2365660006, folderID, entryID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * SshConnectDynamicAdvanced is the dynamic-inventory twin of
 * SshConnectAdvanced. Same semantics: each override is independent,
 * empty string = fall through to normal resolution.
 * @param {string} folderID
 * @param {string} entryID
 * @param {string} overrideCredentialID
 * @param {string} overrideUsername
 * @param {string} overridePassword
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnectDynamicAdvanced(folderID, entryID, overrideCredentialID, overrideUsername, overridePassword) {
    return $Call.ByID(213420756, folderID, entryID, overrideCredentialID, overrideUsername, overridePassword).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * SshConnectDynamicWithJumpOverride adds jump host overrides to the
 * advanced flow. jumpHostOverride is "[user@]host[:port]" (empty =
 * keep the host parsed from Ansible vars / inherited from folder).
 * jumpCredentialOverride is a credential id that replaces the
 * per-folder jump credential for this connect only.
 * @param {string} folderID
 * @param {string} entryID
 * @param {string} overrideCredentialID
 * @param {string} overrideUsername
 * @param {string} overridePassword
 * @param {string} jumpHostOverride
 * @param {string} jumpCredentialOverride
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnectDynamicWithJumpOverride(folderID, entryID, overrideCredentialID, overrideUsername, overridePassword, jumpHostOverride, jumpCredentialOverride) {
    return $Call.ByID(2027454034, folderID, entryID, overrideCredentialID, overrideUsername, overridePassword, jumpHostOverride, jumpCredentialOverride).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * SshConnectDynamicWithOverride mirrors SshConnectWithOverride for
 * dynamic-inventory entries - same one-shot credential override that
 * doesn't touch any persisted state.
 * @param {string} folderID
 * @param {string} entryID
 * @param {string} overrideCredentialID
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnectDynamicWithOverride(folderID, entryID, overrideCredentialID) {
    return $Call.ByID(4083963130, folderID, entryID, overrideCredentialID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * SshConnectWithOverride is identical to SshConnect except it forces
 * the credential used for the target hop to overrideCredentialID for
 * this one attempt. The connection's persisted auth_ref is NOT
 * modified - the override is in-memory only and never survives the
 * call. Empty overrideCredentialID falls through to the regular
 * resolution path (same as SshConnect).
 * @param {string} connectionID
 * @param {string} overrideCredentialID
 * @returns {$CancellablePromise<$models.SshConnectResult | null>}
 */
export function SshConnectWithOverride(connectionID, overrideCredentialID) {
    return $Call.ByID(1062713217, connectionID, overrideCredentialID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType104($result);
    }));
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function SshDisconnect(sessionID) {
    return $Call.ByID(3056454639, sessionID);
}

/**
 * SshGetConnectDebug returns the buffered debug lines for the most
 * recent connect attempt against this connectionID. Empty slice if
 * nothing buffered (verbose off, or no recent attempt).
 * @param {string} connectionID
 * @returns {$CancellablePromise<string[]>}
 */
export function SshGetConnectDebug(connectionID) {
    return $Call.ByID(5085870, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * SshGetScrollback returns the buffered PTY output for a session plus the
 * cumulative-byte watermark. Called by Terminal.svelte on mount: subscribe
 * to pty_output first, then call this - events with cum ≤ watermark are
 * already in the snapshot, partials straddling the watermark are trimmed.
 * @param {string} sessionID
 * @returns {$CancellablePromise<$models.ScrollbackSnapshot>}
 */
export function SshGetScrollback(sessionID) {
    return $Call.ByID(1742244993, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType65($result);
    }));
}

/**
 * SshLaunchBrowser opens a browser pointed at the given SOCKS5 forward.
 * Respects the user's `preferred_browser_path` setting if present;
 * otherwise platform default detection (see internal/ssh/browser.go).
 * @param {string} forwardID
 * @param {string} url
 * @returns {$CancellablePromise<$models.BrowserLaunchResult | null>}
 */
export function SshLaunchBrowser(forwardID, url) {
    return $Call.ByID(2568987350, forwardID, url).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType106($result);
    }));
}

/**
 * SshLaunchInSystemTerminal opens the OS terminal with the equivalent
 * `ssh ...` command preloaded so the user can run it with their own
 * agent / key material (useful for opkssh-cli-based scripts, or when
 * the user wants to keep a shell open after this app exits).
 * 
 * Best-effort by platform:
 * 
 * 	Windows: prefer Windows Terminal (`wt new-tab -- cmd /k ...`);
 * 	         fall back to plain cmd if wt isn't installed.
 * 	macOS:   `osascript` -> `Terminal` -> do script.
 * 	Linux:   tries xterm / gnome-terminal / konsole in order. If
 * 	         none is found, returns an error and the UI falls back
 * 	         to the existing "copy to clipboard" path.
 * @param {string} connectionID
 * @returns {$CancellablePromise<void>}
 */
export function SshLaunchInSystemTerminal(connectionID) {
    return $Call.ByID(600439124, connectionID);
}

/**
 * @param {string} sessionID
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<void>}
 */
export function SshResize(sessionID, cols, rows) {
    return $Call.ByID(4102843349, sessionID, cols, rows);
}

/**
 * SshRespondHostKey is called by the frontend after the user decides on a
 * host key challenge. remember=true persists the key to known_hosts.
 * @param {string} challengeID
 * @param {boolean} accept
 * @param {boolean} remember
 * @param {string} hostname
 * @param {number} port
 * @param {string} keyType
 * @param {string} keyB64
 * @param {string} fingerprint
 * @returns {$CancellablePromise<void>}
 */
export function SshRespondHostKey(challengeID, accept, remember, hostname, port, keyType, keyB64, fingerprint) {
    return $Call.ByID(3539493659, challengeID, accept, remember, hostname, port, keyType, keyB64, fingerprint);
}

/**
 * SshServerStats runs a one-shot read-only health probe (load / memory /
 * disk / logged-in users) on a side channel of the session's SSH client and
 * returns the parsed snapshot. Used by the optional status-bar readout for
 * the focused session; the frontend only calls this when the feature is on
 * and a terminal pane is focused, so no probing happens otherwise. Returns
 * OK=false stats (not an error) for a host that answered but yielded no
 * parseable metrics; errors only when the session/channel is unavailable.
 * @param {string} sessionID
 * @returns {$CancellablePromise<ssh$0.ServerStats | null>}
 */
export function SshServerStats(sessionID) {
    return $Call.ByID(3459360229, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType108($result);
    }));
}

/**
 * SshSystemCommand returns the same invocation as sshSystemArgv but
 * as a single space-joined string for display / copy purposes. Do
 * NOT pass the returned value to a shell or to exec - hostname and
 * username are unquoted here. Use sshSystemArgv for any exec path.
 * @param {string} connectionID
 * @returns {$CancellablePromise<string>}
 */
export function SshSystemCommand(connectionID) {
    return $Call.ByID(2388748401, connectionID);
}

/**
 * @param {string} sessionID
 * @param {string} dataB64
 * @returns {$CancellablePromise<void>}
 */
export function SshWrite(sessionID, dataB64) {
    return $Call.ByID(3443123936, sessionID, dataB64);
}

/**
 * SyncAutoApplySet toggles automatic background pull (apply incoming
 * changes without asking). Independent of auto-push; meaningful only
 * when auto sync is on.
 * @param {boolean} enabled
 * @returns {$CancellablePromise<void>}
 */
export function SyncAutoApplySet(enabled) {
    return $Call.ByID(1662284753, enabled);
}

/**
 * SyncAutoSet flips the auto-sync loop (push-on-change + periodic
 * remote check) and its check interval.
 * @param {boolean} enabled
 * @param {number} checkMinutes
 * @returns {$CancellablePromise<void>}
 */
export function SyncAutoSet(enabled, checkMinutes) {
    return $Call.ByID(501558299, enabled, checkMinutes);
}

/**
 * @returns {$CancellablePromise<$models.SyncConfig>}
 */
export function SyncConfigGet() {
    return $Call.ByID(2749435992).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType109($result);
    }));
}

/**
 * SyncConfigSet stores the sync settings. Empty password/passphrase
 * keeps the existing vault value (the UI sends blanks unless changed).
 * @param {string} url
 * @param {string} username
 * @param {string} webdavPassword
 * @param {string} passphrase
 * @returns {$CancellablePromise<void>}
 */
export function SyncConfigSet(url, username, webdavPassword, passphrase) {
    return $Call.ByID(634578628, url, username, webdavPassword, passphrase);
}

/**
 * SyncPull downloads the remote snapshot and stages it through the
 * backup-restore path. The user must quit + reopen to apply; the
 * restored profile carries its own sync_generation, so no local
 * bookkeeping here.
 * @returns {$CancellablePromise<syncer$0.PullResult | null>}
 */
export function SyncPull() {
    return $Call.ByID(2202462297).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType111($result);
    }));
}

/**
 * SyncPullLive applies a pulled profile WITHOUT a restart: the store is
 * mirrored into the running database (SSH sessions survive) and the
 * vault's secrets are re-encrypted under the local key and merged into
 * the unlocked vault. The frontend reloads its stores afterward.
 * 
 * The vault merge needs the snapshot's vault passphrase. For a single
 * user with the same passphrase everywhere that's the local one (read
 * from the machine sidecar), so it's silent. If that doesn't open the
 * snapshot's vault (different passphrase across machines), the store is
 * still applied and VaultRestartNeeded is returned so the caller can
 * stage a restart for the secrets - never a passphrase prompt.
 * @returns {$CancellablePromise<$models.SyncPullLiveResult | null>}
 */
export function SyncPullLive() {
    return $Call.ByID(3365936299).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType113($result);
    }));
}

/**
 * SyncPush seals + uploads the profile. The new generation is
 * persisted BEFORE sealing so the uploaded snapshot carries it (a
 * machine that pulls it comes up in-sync); on failure it's rolled
 * back so the next attempt's guard math stays right.
 * @param {boolean} force
 * @returns {$CancellablePromise<syncer$0.PushResult | null>}
 */
export function SyncPush(force) {
    return $Call.ByID(2771076962, force).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType115($result);
    }));
}

/**
 * @param {$models.SyncSftpConfigInput} $in
 * @returns {$CancellablePromise<void>}
 */
export function SyncSftpConfigSet($in) {
    return $Call.ByID(3288997271, $in);
}

/**
 * @returns {$CancellablePromise<$models.SyncStatusResult | null>}
 */
export function SyncStatus() {
    return $Call.ByID(683898348).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType117($result);
    }));
}

/**
 * SyncTransportSet selects the sync backend ("webdav" or "sftp").
 * @param {string} transport
 * @returns {$CancellablePromise<void>}
 */
export function SyncTransportSet(transport) {
    return $Call.ByID(3151017917, transport);
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<$models.TcpdumpActiveInfo | null>}
 */
export function TcpdumpActiveForSession(sessionID) {
    return $Call.ByID(2189909377, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType119($result);
    }));
}

/**
 * TcpdumpCheckRoute runs `ip route get` on the session's host for each
 * query and returns the egress interface + source address the kernel
 * would pick. This is the active confirmation behind an insight's
 * "Check route" button: it shows whether traffic to a peer actually
 * leaves the expected interface with the expected source IP, which is
 * the ground truth for the wrong-interface / 0.0.0.0-bind class of
 * problems the passive analyzer flags. No sudo required.
 * @param {string} sessionID
 * @param {$models.TcpdumpRouteQuery[]} queries
 * @returns {$CancellablePromise<ssh$0.RouteResult[]>}
 */
export function TcpdumpCheckRoute(sessionID, queries) {
    return $Call.ByID(1969483991, sessionID, queries).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType121($result);
    }));
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<string[]>}
 */
export function TcpdumpListInterfaces(sessionID) {
    return $Call.ByID(3944695350, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * @param {string} sessionID
 * @returns {$CancellablePromise<$models.TcpdumpProbeResult | null>}
 */
export function TcpdumpProbe(sessionID) {
    return $Call.ByID(4172900560, sessionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType123($result);
    }));
}

/**
 * @param {string} dumpID
 * @param {string} password
 * @returns {$CancellablePromise<void>}
 */
export function TcpdumpProvidePassword(dumpID, password) {
    return $Call.ByID(1474369498, dumpID, password);
}

/**
 * TcpdumpSnapshot returns the server-side packet history for a running
 * capture. A window attaching mid-capture (after a detach moved the
 * session here) calls this to recover the packets it missed, then
 * dedupes the live stream against Cum. Empty result if the dump is gone.
 * @param {string} dumpID
 * @returns {$CancellablePromise<$models.TcpdumpSnapshotResult | null>}
 */
export function TcpdumpSnapshot(dumpID) {
    return $Call.ByID(2792891634, dumpID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType125($result);
    }));
}

/**
 * TcpdumpStart launches a capture and returns its dumpID. Live lines
 * arrive as `tcpdump_line:<dumpID>` events; lifecycle transitions
 * (needs_password / started / password_rejected / error / ended) as
 * `tcpdump_event:<dumpID>` events with payload {event, msg}.
 * @param {$models.TcpdumpStartInput} $in
 * @returns {$CancellablePromise<string>}
 */
export function TcpdumpStart($in) {
    return $Call.ByID(4233083294, $in);
}

/**
 * @param {string} dumpID
 * @returns {$CancellablePromise<void>}
 */
export function TcpdumpStop(dumpID) {
    return $Call.ByID(3050648742, dumpID);
}

/**
 * URLSchemeStatus returns a short status string describing the
 * currently-registered handler ("" = not registered, otherwise an
 * OS-specific identifier like the launch command or
 * `ssh-tool-url.desktop`).
 * @returns {$CancellablePromise<string>}
 */
export function URLSchemeStatus() {
    return $Call.ByID(1016096919);
}

/**
 * UnpinConnection deletes the pin mapping AND the underlying
 * connection. The next inventory refresh re-includes the original
 * external_id as a dynamic ghost. Returns the dynamic folder id so the
 * frontend knows which folder to refresh-now.
 * @param {string} connectionID
 * @returns {$CancellablePromise<string>}
 */
export function UnpinConnection(connectionID) {
    return $Call.ByID(2479323493, connectionID);
}

/**
 * @returns {$CancellablePromise<boolean>}
 */
export function VaultAutoUnlock() {
    return $Call.ByID(2990838000);
}

/**
 * VaultChangePassphrase rotates the master passphrase. Vault must be
 * unlocked. The old passphrase is independently re-verified against
 * the on-disk file before any mutation.
 * @param {string} oldPassphrase
 * @param {string} newPassphrase
 * @returns {$CancellablePromise<void>}
 */
export function VaultChangePassphrase(oldPassphrase, newPassphrase) {
    return $Call.ByID(3598395459, oldPassphrase, newPassphrase);
}

/**
 * @param {string} passphrase
 * @param {boolean} rememberOnMachine
 * @returns {$CancellablePromise<void>}
 */
export function VaultInit(passphrase, rememberOnMachine) {
    return $Call.ByID(3081034657, passphrase, rememberOnMachine);
}

/**
 * @param {boolean} forgetSidecar
 * @returns {$CancellablePromise<void>}
 */
export function VaultLock(forgetSidecar) {
    return $Call.ByID(3103429952, forgetSidecar);
}

/**
 * @returns {$CancellablePromise<creds$0.Status>}
 */
export function VaultStatus() {
    return $Call.ByID(704376449).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType126($result);
    }));
}

/**
 * @param {string} passphrase
 * @param {boolean} rememberOnMachine
 * @returns {$CancellablePromise<void>}
 */
export function VaultUnlock(passphrase, rememberOnMachine) {
    return $Call.ByID(1125475311, passphrase, rememberOnMachine);
}

/**
 * VncClose tears down a console: drops the metadata and disconnects any
 * SSH session the console owned (the tunnel). The bridge token is
 * single-use and already consumed or GC'd.
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function VncClose(sessionID) {
    return $Call.ByID(4250764390, sessionID);
}

/**
 * VncLastError returns (and clears) the last upstream-open error for a
 * console, by sessionID. The frontend calls this when a console
 * disconnects/closes while still connecting, to show WHY (e.g. a jump-host
 * auth failure) instead of a generic "connection closed". Empty if none.
 * @param {string} sessionID
 * @returns {$CancellablePromise<string>}
 */
export function VncLastError(sessionID) {
    return $Call.ByID(1620004904, sessionID);
}

/**
 * VncOpenConnection opens a console for a generic (non-Proxmox) saved
 * connection. Resolves vnc_port + vnc_use_tunnel + the optional vault
 * VNC password. When tunneling, it opens a dedicated SSH session the
 * console owns (torn down on VncClose) and dials the RFB port through
 * it; otherwise it dials host:vnc_port directly.
 * @param {string} connectionID
 * @returns {$CancellablePromise<$models.VncSession | null>}
 */
export function VncOpenConnection(connectionID) {
    return $Call.ByID(40425702, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType128($result);
    }));
}

/**
 * VncOpenPinnedProxmox opens a Proxmox console for a PINNED connection -
 * one promoted from a Proxmox dynamic entry. Pinning drops the cached
 * entry (with its node/raw), so we recover the link from
 * pinned_dynamic_entries (folder + external_id like "qemu:114"/"lxc:110")
 * and look up the current node by vmid via /cluster/resources.
 * @param {string} connectionID
 * @returns {$CancellablePromise<$models.VncSession | null>}
 */
export function VncOpenPinnedProxmox(connectionID) {
    return $Call.ByID(3943515215, connectionID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType128($result);
    }));
}

/**
 * VncOpenProxmox opens a console for a Proxmox VM/LXC dynamic entry. It
 * resolves the folder's base_url + API token, reads node/vmid/type from
 * the entry, and registers a bridge upstream that runs the vncproxy POST
 * + vncwebsocket on connect. Returns the ws URL the webview connects to.
 * @param {string} folderID
 * @param {string} entryID
 * @returns {$CancellablePromise<$models.VncSession | null>}
 */
export function VncOpenProxmox(folderID, entryID) {
    return $Call.ByID(2470244949, folderID, entryID).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType128($result);
    }));
}

/**
 * VncSessionList returns every live VNC console with a freshly-minted ws
 * token. A detached window calls this after a tab tear-off to re-register
 * its VNC tabs and reconnect noVNC - mirrors LocalShellList for PTYs.
 * @returns {$CancellablePromise<$models.VncSession[]>}
 */
export function VncSessionList() {
    return $Call.ByID(1649682954).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType129($result);
    }));
}

/**
 * WindowAcceptTabDrag is called by the main window when a tab is dropped onto
 * its tab bar. Returns the pending drag payload and clears it.
 * @returns {$CancellablePromise<$models.TabDragPayload | null>}
 */
export function WindowAcceptTabDrag() {
    return $Call.ByID(1574582034).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType131($result);
    }));
}

/**
 * WindowCancelTabDrag clears a pending drag without accepting it (drag ended
 * without being dropped on the main window).
 * @returns {$CancellablePromise<void>}
 */
export function WindowCancelTabDrag() {
    return $Call.ByID(4035557260);
}

/**
 * WindowCloseSelf closes the calling window WITHOUT touching its
 * sessions. Used by the redock path - the main window has just
 * claimed ownership, so we clear the detached slot before closing
 * and the WindowClosing handler finds nothing to disconnect.
 * 
 * Plain user-close (clicking the X on a detached window) skips
 * this path and goes through WindowClosing directly, which disconnects.
 * @param {string} windowName
 * @returns {$CancellablePromise<void>}
 */
export function WindowCloseSelf(windowName) {
    return $Call.ByID(2496802621, windowName);
}

/**
 * WindowDetachTab opens a new top-level window carrying just the given
 * tabId. The Svelte app reads ?detached=<tabId> from the URL and
 * renders only that tab's TerminalArea. SSH sessions stay in the
 * shared backend pool, so both windows can keep observing pty_output /
 * session_state events for sessions that belonged to the detached tab.
 * 
 * Returns the new window's name so the frontend can address it.
 * @param {string} tabID
 * @param {string} sessions
 * @param {string} layout
 * @returns {$CancellablePromise<string>}
 */
export function WindowDetachTab(tabID, sessions, layout) {
    return $Call.ByID(3167650081, tabID, sessions, layout);
}

/**
 * WindowDetachTabAt opens a new window for tabID positioned at (screenX, screenY).
 * sessions is a comma-separated list of session IDs that belong to the tab;
 * the detached window uses it to restore only its own sessions.
 * layout is an opaque base64 blob (see TabDragPayload) carrying the
 * full pane tree so splits / titles / group metadata survive the move.
 * Pass (0,0) for centered placement, "" for sessions to recover all.
 * @param {string} tabID
 * @param {number} screenX
 * @param {number} screenY
 * @param {string} sessions
 * @param {string} layout
 * @returns {$CancellablePromise<string>}
 */
export function WindowDetachTabAt(tabID, screenX, screenY, sessions, layout) {
    return $Call.ByID(1360732732, tabID, screenX, screenY, sessions, layout);
}

/**
 * WindowRedockTab tells the main window that a detached tab should come back.
 * sessions is the comma-separated list of session IDs the detached window owns.
 * layout is an opaque base64 blob with the pane tree so the main
 * window can restore splits / titles instead of reflattening sessions
 * into separate tabs. The main window uses them to reconstruct its pane tabs.
 * @param {string} tabID
 * @param {string} sessions
 * @param {string} layout
 * @returns {$CancellablePromise<void>}
 */
export function WindowRedockTab(tabID, sessions, layout) {
    return $Call.ByID(1336970294, tabID, sessions, layout);
}

/**
 * WindowStartTabDrag registers an in-flight tab drag originating from a
 * detached window. The main window polls this on drop to reclaim the tab.
 * layout is an opaque base64 blob carrying the pane tree so splits and
 * titles survive the redock.
 * @param {string} tabID
 * @param {string} sessions
 * @param {string} layout
 * @returns {$CancellablePromise<void>}
 */
export function WindowStartTabDrag(tabID, sessions, layout) {
    return $Call.ByID(1852490528, tabID, sessions, layout);
}

/**
 * @param {string} name
 * @param {string} layoutJSON
 * @returns {$CancellablePromise<store$0.Workspace | null>}
 */
export function WorkspaceCreate(name, layoutJSON) {
    return $Call.ByID(2050853866, name, layoutJSON).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType133($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function WorkspaceDelete(id) {
    return $Call.ByID(2813361881, id);
}

/**
 * WorkspaceTouchLastOpened bumps last_opened_at after the frontend
 * successfully restores a workspace. Best-effort.
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function WorkspaceTouchLastOpened(id) {
    return $Call.ByID(3734740352, id);
}

/**
 * @param {string} id
 * @param {string} name
 * @param {string} layoutJSON
 * @returns {$CancellablePromise<store$0.Workspace | null>}
 */
export function WorkspaceUpdate(id, name, layoutJSON) {
    return $Call.ByID(1777295735, id, name, layoutJSON).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType133($result);
    }));
}

/**
 * WorkspaceListResult mirrors store.Workspace but with the layout
 * pre-parsed by the frontend; backend stays opaque.
 * @returns {$CancellablePromise<store$0.Workspace[]>}
 */
export function WorkspacesList() {
    return $Call.ByID(3941184719).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType134($result);
    }));
}

// Private type creation functions
const $$createType0 = $Create.Array($Create.Any);
const $$createType1 = $models.AppVersionInfo.createFrom;
const $$createType2 = store$0.AuditEvent.createFrom;
const $$createType3 = $Create.Array($$createType2);
const $$createType4 = $models.AutoBackupPrefs.createFrom;
const $$createType5 = $models.BackupCreateResult.createFrom;
const $$createType6 = $Create.Nullable($$createType5);
const $$createType7 = backup$0.Info.createFrom;
const $$createType8 = $Create.Array($$createType7);
const $$createType9 = ssh$0.BatchHostResult.createFrom;
const $$createType10 = $Create.Array($$createType9);
const $$createType11 = $Create.Map($Create.Any, $$createType0);
const $$createType12 = $models.UpdateCheckResult.createFrom;
const $$createType13 = $models.ConnectionCopyInfo.createFrom;
const $$createType14 = $Create.Nullable($$createType13);
const $$createType15 = $models.ConnectionsBatchUpdateResult.createFrom;
const $$createType16 = $Create.Nullable($$createType15);
const $$createType17 = store$0.Connection.createFrom;
const $$createType18 = $Create.Nullable($$createType17);
const $$createType19 = $Create.Array($$createType17);
const $$createType20 = store$0.ResolvedSettings.createFrom;
const $$createType21 = $Create.Nullable($$createType20);
const $$createType22 = store$0.CredentialFolder.createFrom;
const $$createType23 = $Create.Nullable($$createType22);
const $$createType24 = $Create.Array($$createType22);
const $$createType25 = creds$0.CreateResult.createFrom;
const $$createType26 = $Create.Nullable($$createType25);
const $$createType27 = store$0.CredentialRef.createFrom;
const $$createType28 = $Create.Nullable($$createType27);
const $$createType29 = store$0.CredentialHistoryEntry.createFrom;
const $$createType30 = $Create.Array($$createType29);
const $$createType31 = $Create.Array($$createType27);
const $$createType32 = store$0.CredentialSecretHistoryEntry.createFrom;
const $$createType33 = $Create.Array($$createType32);
const $$createType34 = store$0.UsageRef.createFrom;
const $$createType35 = $Create.Array($$createType34);
const $$createType36 = updater$0.DownloadResult.createFrom;
const $$createType37 = $Create.Nullable($$createType36);
const $$createType38 = store$0.DynamicEntry.createFrom;
const $$createType39 = $Create.Array($$createType38);
const $$createType40 = store$0.Folder.createFrom;
const $$createType41 = $Create.Nullable($$createType40);
const $$createType42 = store$0.DynamicFolder.createFrom;
const $$createType43 = $Create.Nullable($$createType42);
const $$createType44 = $Create.Array($$createType42);
const $$createType45 = $models.ExportSubtreeResult.createFrom;
const $$createType46 = $Create.Nullable($$createType45);
const $$createType47 = $models.ReleaseNotes.createFrom;
const $$createType48 = $Create.Array($$createType40);
const $$createType49 = ssh$0.ForwardStatus.createFrom;
const $$createType50 = $Create.Array($$createType49);
const $$createType51 = store$0.PortForward.createFrom;
const $$createType52 = $Create.Nullable($$createType51);
const $$createType53 = $Create.Array($$createType51);
const $$createType54 = $Create.Nullable($$createType49);
const $$createType55 = httpc$0.Response.createFrom;
const $$createType56 = $Create.Nullable($$createType55);
const $$createType57 = $models.ImagePayload.createFrom;
const $$createType58 = $Create.Nullable($$createType57);
const $$createType59 = store$0.ImageSummary.createFrom;
const $$createType60 = $Create.Array($$createType59);
const $$createType61 = exporter$0.ImportSummary.createFrom;
const $$createType62 = $Create.Nullable($$createType61);
const $$createType63 = $models.LoadTextFileResult.createFrom;
const $$createType64 = $Create.Nullable($$createType63);
const $$createType65 = $models.ScrollbackSnapshot.createFrom;
const $$createType66 = $models.LocalShellInfo.createFrom;
const $$createType67 = $Create.Array($$createType66);
const $$createType68 = $models.LocalShellOpenResult.createFrom;
const $$createType69 = $Create.Nullable($$createType68);
const $$createType70 = mobaxterm$0.Summary.createFrom;
const $$createType71 = $Create.Nullable($$createType70);
const $$createType72 = $models.NetworkProfileInfo.createFrom;
const $$createType73 = $Create.Nullable($$createType72);
const $$createType74 = wg$0.Status.createFrom;
const $$createType75 = $Create.Nullable($$createType74);
const $$createType76 = $Create.Array($$createType72);
const $$createType77 = $models.OpksshCertStatusResult.createFrom;
const $$createType78 = $Create.Nullable($$createType77);
const $$createType79 = $models.ProfileStats.createFrom;
const $$createType80 = $Create.Nullable($$createType79);
const $$createType81 = puttyreg$0.Summary.createFrom;
const $$createType82 = $Create.Nullable($$createType81);
const $$createType83 = rdm$0.Summary.createFrom;
const $$createType84 = $Create.Nullable($$createType83);
const $$createType85 = $models.RecordingState.createFrom;
const $$createType86 = $Create.Array($$createType85);
const $$createType87 = $Create.Nullable($$createType85);
const $$createType88 = recorder$0.FileInfo.createFrom;
const $$createType89 = $Create.Array($$createType88);
const $$createType90 = $models.SftpListResult.createFrom;
const $$createType91 = $Create.Nullable($$createType90);
const $$createType92 = $models.SftpPreview.createFrom;
const $$createType93 = $Create.Nullable($$createType92);
const $$createType94 = ssh$0.SftpEntry.createFrom;
const $$createType95 = $Create.Nullable($$createType94);
const $$createType96 = store$0.Snippet.createFrom;
const $$createType97 = $Create.Nullable($$createType96);
const $$createType98 = $Create.Array($$createType96);
const $$createType99 = $models.ActiveSessionInfo.createFrom;
const $$createType100 = $Create.Array($$createType99);
const $$createType101 = sshconfig$0.Summary.createFrom;
const $$createType102 = $Create.Nullable($$createType101);
const $$createType103 = $models.SshConnectResult.createFrom;
const $$createType104 = $Create.Nullable($$createType103);
const $$createType105 = $models.BrowserLaunchResult.createFrom;
const $$createType106 = $Create.Nullable($$createType105);
const $$createType107 = ssh$0.ServerStats.createFrom;
const $$createType108 = $Create.Nullable($$createType107);
const $$createType109 = $models.SyncConfig.createFrom;
const $$createType110 = syncer$0.PullResult.createFrom;
const $$createType111 = $Create.Nullable($$createType110);
const $$createType112 = $models.SyncPullLiveResult.createFrom;
const $$createType113 = $Create.Nullable($$createType112);
const $$createType114 = syncer$0.PushResult.createFrom;
const $$createType115 = $Create.Nullable($$createType114);
const $$createType116 = $models.SyncStatusResult.createFrom;
const $$createType117 = $Create.Nullable($$createType116);
const $$createType118 = $models.TcpdumpActiveInfo.createFrom;
const $$createType119 = $Create.Nullable($$createType118);
const $$createType120 = ssh$0.RouteResult.createFrom;
const $$createType121 = $Create.Array($$createType120);
const $$createType122 = $models.TcpdumpProbeResult.createFrom;
const $$createType123 = $Create.Nullable($$createType122);
const $$createType124 = $models.TcpdumpSnapshotResult.createFrom;
const $$createType125 = $Create.Nullable($$createType124);
const $$createType126 = creds$0.Status.createFrom;
const $$createType127 = $models.VncSession.createFrom;
const $$createType128 = $Create.Nullable($$createType127);
const $$createType129 = $Create.Array($$createType127);
const $$createType130 = $models.TabDragPayload.createFrom;
const $$createType131 = $Create.Nullable($$createType130);
const $$createType132 = store$0.Workspace.createFrom;
const $$createType133 = $Create.Nullable($$createType132);
const $$createType134 = $Create.Array($$createType132);
